<section class="flex flex-col md:flex-row h-screen items-center">

  <div class="bg-green-600 hidden lg:block w-full md:w-1/2 xl:w-2/3 h-screen">
    <%= image_tag "knitting1.jpg", class: "w-full h-full"%>
  </div>

  <div class="bg-white w-full md:max-w-md lg:max-w-full md:mx-auto md:mx-0 md:w-1/2 xl:w-1/3 h-screen px-6 lg:px-16 xl:px-12 flex items-center justify-center">

    <div class="w-full h-100">

      <h1 class="text-xl md:text-2xl font-bold leading-tight mt-12 font-body text-center"><%= t('.title') %></h1>

      <%= form_with url: login_path, local: true do |f| %>
        <div>
          <%= f.label :email , User.human_attribute_name(:email), class: 'block text-gray-700' %>
          <%= f.email_field :email, class: 'font-body form-control w-full px-4 py-3 rounded-lg bg-gray-200 mt-2 border focus:border-green-500 focus:bg-white focus:outline-none' %>
        </div>

        <div class="mt-4">
          <%= f.label :password , User.human_attribute_name(:password), class: 'block text-gray-700' %>
          <%= f.password_field :password, class: 'font-body form-control w-full px-4 py-3 rounded-lg bg-gray-200 mt-2 border focus:border-green-500 focus:bg-white focus:outline-none' %>
        </div>

        <button type="submit" class="w-full block bg-green-500 hover:bg-green-400 focus:bg-green-400 text-white font-semibold rounded-lg px-4 py-3 mt-6 font-body"><%= t('.title') %>する>></button>
      <% end %>
        <div class="text-right mt-11 text-sm font-semibold text-gray-700 font-body">
          <%= link_to "会員登録お済みでないようでしたらこちらから🌼", new_user_path, method: :get, class: 'hover:bg-green-300 py-3 px-3 rounded-full'%>
        </div>
    </div>
  </div>

</section>