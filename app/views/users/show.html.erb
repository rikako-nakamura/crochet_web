<div class="container px-5 py-24 mx-auto">
  <div class="flex flex-col text-center w-full mb-10 bg-yellow-100">
    <%= image_tag "flower1.png"%>
    <h1 class="lg:text-3xl text-xl title-font mb-4 text-gray-900 font-body"><%= "#{current_user.name}さん、何を作りますか？" %></h1>
    <p class="lg:w-2/3 mx-auto leading-relaxed text-sm font-body lg:text-lg">かぎ針で編んだ作品をコレクションしましょう!</p>
    <%= image_tag "flower1.png"%>
  </div>
  <%= render 'shared/buttons' %>
  <div class="mx-auto">
    <p class="text-center text-green-500 lg:text-3xl text-xl mt-11 font-body"><%= "🌼#{current_user.name}さんが投稿した作品一覧🌼" %></p>
    <div class="flex justify-center flex-wrap">
      <% if @posts.present? %>
        <% @posts.each do |post| %>
        <section class="lg:w-1/4 w-full text-gray-600 body-font">
          <div class="container px-5 py-12 mx-auto">
            <div class="flex flex-wrap -m-4">
              <div class="p-4 w-full">
                <div class="h-full border-2 border-gray-200 border-opacity-60 rounded-lg overflow-hidden">
                  <%= image_tag post.post_image_url, class: 'lg:h-80 md:h-36 w-full object-cover object-center'%>
                  <div class="p-6 font-body">
                    <div class="text-sm text-gray-900 mb-3 mr-3" id="post-id-<%= post.id %>">
                      <%= "#{post.body}"%>
                    </div>
                    <div class="flex items-center flex-wrap ">
                      <%= link_to edit_post_path(post), class: 'text-green-500 inline-flex items-center md:mb-2 lg:mb-0 font-body' do%>
                        編集する
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z" />
                        </svg>
                      <% end %>
                      <%= link_to '削除する', post, method: :delete, data: { confirm: "投稿を削除してよろしいですか？"}, class: 'font-body w-2/5 text-center sm:w-auto px-5 py-2 ml-20 text-sm font-medium focus:ring-2 focus:ring-offset-2 focus:ring-offset-white focus:ring-gray-900 focus:outline-none transition-colors duration-200 rounded-full block bg-transparent hover:bg-red-700 border border-red-700 text-red-700 hover:text-red-200'%>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </section> 
        <% end %>
      <% else %>
        <p class="text-center text-green-500 font-body lg:text-3xl mt-11 leading-normal"> 投稿していません。<br><%="#{current_user.name}さんの編み物をぜひみたいです🌷" %></p>
      <% end %>
    </div>
    <%= paginate @posts %>
  </div>
</div>