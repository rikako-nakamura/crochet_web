<div>
  <h1 class="text-center text-green-500 text-6xl mt-40 mb-20"><%= "#{@user.name}さん、何を作りますか？" %></h1>
</div>

<div class="justify-center mx-auto w-3/4	h-96	bg-yellow-200">
  <div class="col-sm-12 col-lg-4 mb-3 ">
    <% if @posts.present? %>
      <div class= "flex">
        <% @posts.each do |post| %>
          <%= image_tag post.post_image_url, size: '300x200'%>
        <% end %>
      </div>
    <% else %>
      <p><%= "投稿していません" %></p>
    <% end %>
  </div>
<%= paginate @posts %>
</div>

<div class= "flex">
  <div class="mt-4">
    <button class="text-green-500 hover:text-white hover:bg-pink-500 border border-pink-500 text-xs font-semibold rounded-full px-4 py-1 leading-normal">色塗りした一覧</button>
  </div>

  <div class="mt-4">
    <button class="text-green-500 hover:text-white hover:bg-pink-500 border border-pink-500 text-xs font-semibold rounded-full px-4 py-1 leading-normal">保存した投稿作品一覧</button>
  </div>
</div>