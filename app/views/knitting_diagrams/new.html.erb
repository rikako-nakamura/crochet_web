<div class="container px-5 py-24 mx-auto">
  <div class="flex flex-col text-center w-full mb-10 bg-pink-100">
    <%= image_tag "flower2.png"%>
    <h1 class="text-3xl font-medium title-font mb-4 text-gray-900 font-body ">Let's drawing Knitting diagrams</h1>
    <p class="lg:w-2/3 mx-auto leading-relaxed text-base font-body text-lg">編み図を図形に例えました。<br>さまざまな色をクリックでイメージできます🌼</p>
    <%= image_tag "flower2.png"%>
  </div>

  <section class="text-gray-600 body-font flex justify-center text-center">
    <div>
      <%= form_with model: @knitting_diagram, local: true do |f| %>
        <div class="lg:w-full flex flex-wrap font-body">
          <div class="flex flex-shrink-0 rounded-lg object-cover object-center mb-4 mt-10 justify-around lg:pl-10 lg:py-6 my-auto">
            <%= f.color_field :square_outer, {class: 'square_outer'} %>
            <%= f.color_field :square_center, {class: 'square_center mt-12'} %>
            <%= f.color_field :square_inner, {class: 'square_inner mt-24'} %>
          </div>
					<% if current_user %>
            <div class="flex p-2 justify-center lg:pl-10 mx-auto my-auto">
              <button type= "submit">
                <%= f.submit (t '.submit'), class: 'w-36 h-36 bg-yellow-300 p-2 rounded-full hover:bg-yellow-400 text-white font-semibold' %>
              </button>
            </div>
          <% else %>
            <div class="p-2 justify-center lg:pl-10 mx-auto my-auto">
              <div class="p-2 justify-center space-x-16">
                <%= link_to new_user_path do %>
                  <button class="w-36 h-36 bg-red-300 p-2 rounded-full hover:bg-red-400 text-white font-semibold">
                    会員登録はこちら
                  </button>
                <% end %>
              </div>
              <p class="text-green-500 lg:text-xl mb-10 mt-3 justify-cente">※マイページに保存する場合会員登録が必要です。</p>
            </div>
          <% end %>
        </div>
      <%end%>
    </div>
  </section>
  <%= javascript_pack_tag 'knitting_diagram/new' %>
</div>
