<h1 class="text-center text-green-500 text-6xl mt-40 mb-20">編み図を図形に例えました。<br>さまざまな色をクリックでイメージできます。</h1>
<md-color-picker id="colorPicker" color-margin="6" colors-per-row="5" color-size="40" default-tint="500" fixedMinHeight="true" palette="material-full" use-spectrum-picker="true" value="#2196f3"></md-color-picker>

<div class="relative justify-end flex">

  <%= form_with model: @knitting_diagram, local: true do |f| %>

    <div class="square_outer">
      <%= f.color_field :square_outer, {class: 'square_outer'} %>
    </div>

    <div class="square_center">
      <%= f.color_field :square_center, {class: 'square_center'} %>
    </div>

    <div class="square_inner">
      <%= f.color_field :square_inner, {class: 'square_inner'} %>
    </div>

    <button class="justify-end items-center bg-red-200 hover:bg-pink-500 text-green-500 py-2 px-4 bg-opacity: 1;" type="submit">
      <%= f.submit (t '.submit') %>
    </button>
    <p class="text-green-500 text-3xl mt-20 mb-20">※保存する場合会員登録が必要です<p>
  <% end %>
</div>
<%= javascript_pack_tag 'knitting_diagram/new' %>
