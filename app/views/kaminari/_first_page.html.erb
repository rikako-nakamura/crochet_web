<%# Link to the "First" page
  - available local variables
    url:           url to the first page
    current_page:  a page object for the currently displayed page
    total_pages:   total number of pages
    per_page:      number of items to fetch per page
    remote:        data-remote
-%>
<span class="first inline-flex items-center py-2 px-4 mr-4 text-sm font-medium text-green-500 bg-white rounded-lg border hover:bg-green-500 hover:text-white font-body">
  <%= link_to_unless current_page.first?, t('view.pagination.first').html_safe, url, remote: remote %>
</span>
